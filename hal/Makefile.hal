HALPARENT = $(FIRMWAREPATH)/hal
HALPATH = $(FIRMWAREPATH)/hal/$(HAL)
VPATH += :$(HALPATH)

#Default stuff
EXTRAINCDIRS += $(HALPARENT)
EXTRAINCDIRS += $(HALPATH)

include $(HALPATH)/Makefile.$(HAL)

CDEFS += -DHAL_TYPE=HAL_$(HAL) -DPLATFORM=$(PLATFORM)